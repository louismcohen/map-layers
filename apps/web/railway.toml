[build]
builder = "railpack"
buildCommand = "pnpm build"

[deploy]
startCommand = "pnpm --filter @map-layers/web exec serve dist -s -l tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
