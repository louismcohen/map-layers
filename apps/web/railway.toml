[build]
builder = "railpack"
buildCommand = "pnpm build"

[deploy]
startCommand = "cd apps/web && ./node_modules/.bin/serve dist --single --no-clipboard --listen tcp://0.0.0.0:$PORT"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
